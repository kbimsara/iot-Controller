/** @type {import('next').NextConfig} */
const nextConfig = {
  // Uncomment the line below to export as a fully static site (GitHub Pages, S3, etc.)
  // output: "export",
};

export default nextConfig;
